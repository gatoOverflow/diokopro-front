// BureauPage.tsx
// Ce composant n'est plus utilisé - la PasswordModal est maintenant appelée depuis sidebar-menu.tsx
// avec les props onSuccess, onCancel, userRole, et enterpriseId

export default function BureauPage() {
  return null;
}